<!-- This is a placeholder for the PNG file.
     Since I can't directly create binary image files through text editing,
     you'll need to convert the SVG to PNG using an image editor or
     online converter, or use the SVG directly by changing the image reference in _Layout.cshtml. --> 